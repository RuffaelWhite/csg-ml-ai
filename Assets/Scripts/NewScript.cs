using UnityEngine;

public class NewScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("NewScript started");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
